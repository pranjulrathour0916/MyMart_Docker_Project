services:
 backend:
  build:
   context: ./martdb
  image: mymartdb
  restart: always
  networks:
   - mynet
 frontend:
  build:
   context: ./My-Mart
  image: mymart
  restart: always
  networks:
   - mynet
  ports:
   - "3000:80"
  depends_on: 
   - backend

networks:
 mynet:
